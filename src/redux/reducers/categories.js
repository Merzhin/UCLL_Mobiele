import { List, Map } from 'immutable';

 const init = {
   categories:[{naam:'overnight stay'},{naam:'transport'},{naam:'activity'},{naam:'food'},{naam:'misc'}],
  
 }
 const reducer = (state=init, action) => {
    return state;
  }
  
  export default reducer